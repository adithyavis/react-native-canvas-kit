# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects — generated on demand by `expo prebuild` (Continuous Native
# Generation). Do not commit; run `expo prebuild` / `expo run:*` to recreate.
/ios
/android

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local
